require 'spec_helper'

describe ActiveTrail do
  it 'has a version number' do
    expect(ActiveTrail::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(true).to eq(true)
  end
end
